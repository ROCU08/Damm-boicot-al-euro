"""Pipeline orchestration for Damm VRP.

Modules:
  - contratos: JSON schemas exchanged between Python orchestrator and C++ binaries.
  - cargar_csv: CSV -> DatosProblema JSON loader.
  - (later) orquestador: runs SA-ruta and N parallel SA-distribucion.
"""
